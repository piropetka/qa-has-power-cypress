# QA-Has-Power-Cypress

## What is needed:

1. Cypress

   ```bash
   $ npm install cypress --save-dev

   ```

## Usage:

1. Run tests:
   ```bash
   $ npx cypress open
   $ npx cypress run --headed --no-exit
   ```
